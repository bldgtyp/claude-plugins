#!/usr/bin/env python3
"""Run the PH-Navigator credential-aware MCP bridge."""

from pathlib import Path
import sys

PLUGIN_ROOT = Path(__file__).resolve().parents[1]
sys.path.insert(0, str(PLUGIN_ROOT / "lib"))

from phn_agent import proxy_main  # noqa: E402


if __name__ == "__main__":
    proxy_main()
