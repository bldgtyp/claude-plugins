---
description: Summarize the current folder's PH-Navigator project and unfinished work
disable-model-invocation: true
---

Follow `/bldgtyp:phn` first. Resolve the current folder's `.phn.json`, then use
the `phn` MCP server to call `get_project`, `list_status_items`, and
`query_unfinished_envelope_work`. Summarize the project, active version, status
items, and unfinished envelope work with exact record names and counts. This is
a read-only command; do not create or mutate a draft.
